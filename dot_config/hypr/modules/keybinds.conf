# Use `wev` to find key names
$mainMod = SUPER

# Applications
bind = $mainMod, T, exec, ghostty
bind = $mainMod, B, exec, google-chrome-stable
bind = $mainMod, E, exec, dolphin
bind = $mainMod, S, exec, spotify
bind = $mainMod, M, exec, teams-for-linux
bind = $mainMod, R, exec, wofi --show drun
bind = $mainMod, P, exec, 1password
bind = $mainMod, D, exec, discord
bind = $mainMod, N, exec, neovide

# Defaults
bind = $mainMod, grave, exit,
bind = $mainMod, Q, killactive,
bind = $mainMod, F, togglefloating,
# bind = $mainMod, P, pseudo, # dwindle
# bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + vim keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Switch workspaces with mainMod + [0-9]
{{- if eq .chezmoi.hostname "anon" }}
bind = $mainMod, 1,  exec, hyprctl dispatch workspace 1,  monitor:DP-3
bind = $mainMod, 2,  exec, hyprctl dispatch workspace 2,  monitor:DP-3
bind = $mainMod, 3,  exec, hyprctl dispatch workspace 3,  monitor:DP-3
bind = $mainMod, 4,  exec, hyprctl dispatch workspace 4,  monitor:DP-3
bind = $mainMod, 5,  exec, hyprctl dispatch workspace 5,  monitor:DP-3
bind = $mainMod, 6,  exec, hyprctl dispatch workspace 6,  monitor:DP-3
bind = $mainMod, 7,  exec, hyprctl dispatch workspace 7,  monitor:DP-3
bind = $mainMod, 8,  exec, hyprctl dispatch workspace 8,  monitor:DP-3
bind = $mainMod, 9,  exec, hyprctl dispatch workspace 9,  monitor:DP-3
bind = $mainMod, 10, exec, hyprctl dispatch workspace 10, monitor:DP-3
{{- end }}
{{- if eq .chezmoi.hostname "nona" }}
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
{{- end }}

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Media keys
bind =  , XF86AudioMute,         exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
binde = , XF86AudioLowerVolume,  exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
binde = , XF86AudioRaiseVolume,  exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind =  , XF86AudioMicMute,      exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
binde = , XF86MonBrightnessDown, exec, brightnessctl set 5%-
binde = , XF86MonBrightnessUp,   exec, brightnessctl set +5%
binde = , 248,                   exec, brightnessctl set --device=platform::kbd_backlight 5%-
binde = , XF86Calculator,        exec, brightnessctl set --device=platform::kbd_backlight 5%+
